version: "3.9"

services:
  auth_service:
    build: .
    ports:
      - "8082:8082"
    command: sbt "runMain auth.AuthMain"

  routing_service:
    build: .
    ports:
      - "8081:8081"
    command: sbt "runMain routing.RoutingMain"